---
import NavBar from './NavBar'
import Logo from './ui/Logo';
import ThemeController from './ThemeController';
---

<header class="flex justify-between md:items-center">
	<section>
		<Logo client:load/>
	</section>
	<section class="flex flex-col items-center justify-between md:flex md:flex-row md:justify-between md:w-full">
		<NavBar client:load/>
		<ThemeController client:load/>
	</section>
</header>
